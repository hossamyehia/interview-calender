<!DOCTYPE html>
<html>
  <head>
    <title>Request Update</title>
    <style>
    body {
        display: flex;
        align-items: center;
        justify-content: center;
        
        width: 100vw;
        height: 100vh;
        margin: 0;
        padding: 0;

        background-color: #2d2e2f;
        
    }
    form{
        width: 400px;
        padding: 25px;
        margin: 0;
        border: 1px solid #2c306f;
        border-radius: 5px;
        text-align: center;

        background-color: #ffffff;
    }
    label{
        font: 27px "Lucida Grande", Helvetica, Arial, sans-serif;
        display: block;
    }
    input[type=email]{
        height: 28px;
        width: 100%;
        margin-bottom: 5px;
    }
    input[type=submit]{
        color: #2c306f;
        cursor: pointer;
        background-color: #ffd048;
        border: none;
        outline: none;
        border-radius: 2px;
        box-shadow: 0.5px 1px 1px 0.5px #fcac00;
        height: 30px;
        width: 50%;
    }
    input[type=submit]:hover{
        background-color: #ffc548;
    }
    input[type="submit"]:active{
        box-shadow: none;
        background-color: #fdc837;
    }
    .alert{
        font: 17px "Lucida Grande", Helvetica, Arial, sans-serif;
        height: 20px;
        margin: 10px;
        padding: 10px;
        color: white;
        text-align: center;
        background-color: #f05731;
        border-radius: 2px;
    }
    </style>
  </head>
  <body>
      <form action="/update" method="post">
        <label for="name">Your Gmail</label>
        <input type="email" id="email" name="email" required>
        <% if(error != "") { %>
            <div class="alert">
                <%- error %>
            </div>
        <% } %>
        <input type="submit" value="Submit">   
      </form>
  </body>
</html>
